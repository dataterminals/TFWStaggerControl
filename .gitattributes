# Normalize line endings. Shell scripts must stay LF so bash runs them cleanly
# despite the machine's global core.autocrlf=true.
* text=auto
*.sh   text eol=lf
*.lua  text eol=lf
*.cs   text eol=lf
*.uasset binary
*.ucas   binary
*.utoc   binary
*.pak    binary
*.png    binary
